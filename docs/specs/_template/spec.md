# Spec — <feature> (Phase <X.n>)

**Status:** draft · **Parent:** `docs/specification.md` <R-*/G-* IDs> · `docs/plan.md` Phase <X.n>

## Scope
What this feature is and is not. (`OUT-*` for explicit exclusions.)

## Requirements
- `R-<area>-<n>` — <testable statement>. MUST/SHOULD/MAY.
- ... (cite the parent requirement each refines)

## Acceptance
How each requirement is verified (test, example, measured number). A result counts only if
converged AND feasible with measured wall-clock (Constitution II, I).

## Decisions & assumptions
- `D-<n>` — <choice + provenance + sensitivity>. Tighten, never relax, the constitution.

## Open questions
- ... (resolve or record in `docs/findings.md`)
